{
  "name": "exchange-rates-frontend",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/with-herd-ca.mjs astro dev",
    "build": "node scripts/with-herd-ca.mjs astro build",
    "preview": "node scripts/with-herd-ca.mjs astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.7",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.6",
    "tailwindcss": "^4.3.3",
    "wrangler": "^4.118.0"
  }
}